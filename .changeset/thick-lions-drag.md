---
"sinequa-mint": minor
---

Add mouse pan (middle-click drag) and zoom (Ctrl/Cmd+wheel, anchored under the cursor) to the document preview. Also fixes a pre-existing bug where the preview's horizontal scrollbar could disappear and the document could get stuck unscrollable left/right past a certain zoom level.
