# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated by docs-overlay from docusaurus/content/ — never edit, never commit.
#
# Anchored with a leading slash on purpose: an unanchored `docs/` would also match
# content/docs/, which is the source, and would hide every tracked file under it.
/docs/
/versioned_docs/
/versioned_sidebars/
/versions.json
/.docs-overlay/
/build-internal
