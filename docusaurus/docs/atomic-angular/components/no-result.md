---
title: NoResult
---

## Overview

The `NoResult` component is an Angular component that displays a stylized message indicating that no results were found.

## Examples

The `NoResultComponent` can be used in the template of a parent component as follows:

```html
<NoResult />
```
